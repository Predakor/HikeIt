import { BarBody } from "./BarBody";
import { BarName } from "./BarName";
import { BarValue } from "./BarValue";

const Bar = {
  Label: BarName,
  Content: BarBody,
  Value: BarValue,
};

export default Bar;
