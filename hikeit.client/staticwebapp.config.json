{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://yourapi.azurewebsites.net/api/"
    }
  ],

  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": ["/api/*", "/assets/*", "/*.css", "/*.js", "/*.png", "/*.svg"]
  }
}
